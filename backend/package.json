{
  "name": "backend",
  "version": "0.1.0",
  "description": "ShiftSync API — team scheduling and shift-swap platform",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.22.2"
  }
}
